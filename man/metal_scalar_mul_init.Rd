% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{metal_scalar_mul_init}
\alias{metal_scalar_mul_init}
\title{Initialize the Metal scalar-multiply pipeline (internal)}
\usage{
metal_scalar_mul_init(metallib_path)
}
\arguments{
\item{metallib_path}{Filesystem path to scalar_mul.metallib.}
}
\value{
TRUE if the pipeline is ready, FALSE otherwise.
}
\description{
Loads the pre-compiled metallib and builds the compute pipeline. Returns
FALSE (never raises) when no Metal device / library / kernel is available,
so the R-side guard can fall back to the CPU path. Idempotent: a second
call with an already-built pipeline returns TRUE immediately.
}
