% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend.R
\name{extend_language}
\alias{extend_language}
\title{Extend the active generator catalog}
\usage{
extend_language(level, name, derivative)
}
\arguments{
\item{level}{Character. One of `"L_0"`, `"L_1"`, `"L_2"`, `"L_3"`.}

\item{name}{Character vector of length 1. The R function name to register.}

\item{derivative}{A function `function(arg_expr, arg_grad)` that returns
an R expression for the chain-rule derivative. `arg_expr` is the call
passed as the function's first argument; `arg_grad` is the recursive
gradient of that argument.}
}
\value{
Invisibly returns `TRUE` on success.
}
\description{
Registers a new generator at the specified language tier. After
registration, [grad()] and [level()] recognise the generator. The
mutation is session-local: reloading the package via
`devtools::load_all()` resets the catalog to defaults.
}
\examples{
\dontrun{
extend_language(
  "L_3", "erf",
  function(x, dx) bquote(2 / sqrt(pi) * exp(-(.(x))^2) * .(dx))
)
grad(quote(erf(v)), "v")
}
}
