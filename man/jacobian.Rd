% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jacobian.R
\name{jacobian}
\alias{jacobian}
\alias{jacobian.default}
\alias{jacobian.function}
\alias{jacobian.call}
\title{Symbolic vector-output Jacobian (S3 generic)}
\usage{
jacobian(x, vars = NULL, ...)

\method{jacobian}{default}(x, vars = NULL, ...)

\method{jacobian}{`function`}(x, vars = NULL, ...)

\method{jacobian}{call}(x, vars, ...)
}
\arguments{
\item{x}{An R object: function or call (from `quote()`).}

\item{vars}{Character vector of variable names. For function input,
defaults to `names(formals(x))`. Single variable in this version.}

\item{...}{Reserved for future use.}
}
\value{
The symbolic Jacobian. Type matches input (function -> function,
  call -> call).
}
\description{
Compute the symbolic Jacobian of a vector-valued function of a vector
variable. For `f : R^n -> R^m` whose body is an explicit assembly of scalar
catalog components (e.g. `c(g_1, ..., g_m)`), returns a function evaluating
to the `m x n` matrix `J[i, j] = d f_i / d v_j`. A scalar-output body
degrades to a `1 x n` row. Stays closed-form (no runtime tape), consistent
with the closure thesis.
}
\examples{
jf <- jacobian(function(v) c(sum(v), sum(v^2)))
jf(c(1, 2, 3))                       # rbind(c(1,1,1), c(2,4,6))

jacobian(quote(c(sum(v), sum(v^2))), "v")
}
